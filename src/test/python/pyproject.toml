[tool.poetry]
name = "mdf4j-test-data"
version = "0.0.0"
description = ""
authors = []
license = "Apache-2.0"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
asammdf = "^7.3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
